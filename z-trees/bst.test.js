describe('binary search tree', () => {
    test('BinaryTree class is constructor', () => {

    })
    test('BinaryTree instance is an object', () => {

    })
    test('BinaryTree instance has a property "root" ', () => {

    })
    test('Node class is constructor of BST nodes', () => {

    })
    test('Node instance has 3 properties: val, left, right', () => {

    })
    test('BinaryTree has a method "insert" ', () => {

    })
    test('If no nodes in tree, inserted node should become a root ', () => {

    })
    test('If root exist, we insert a node to the left if node.val smaller or equal and root left === null', () => {

    })
    test('If root exist, we insert a node to the right if node.val is greater and root.right === null', () => {

    })
    test('If root exist, we insert a node to the right if node.val is greater and root.right === null', () => {

    })
    test('If root has children we traverse a tree until we find the condition', () => {

    })
}) 